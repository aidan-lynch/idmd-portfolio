function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  textSize(32);
  textFont('merriweather') //sets font
text('hi chandru', 10, 30); //types hi chandru
fill(0, 102, 153);
}